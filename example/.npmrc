# react-popper (via reactstrap 9) still declares React <=18 as its peer even though it runs fine on 19;
# npm cannot override a nested peer declaration, so skip strict peer resolution for this demo only.
legacy-peer-deps=true
